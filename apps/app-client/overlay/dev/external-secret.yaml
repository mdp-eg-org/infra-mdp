apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mdpus-client-secret
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  refreshInterval: "1m"
  secretStoreRef:
    name: vault-store
    kind: ClusterSecretStore
  target:
    name: mdpus-client-secret
  dataFrom:
    - extract:
        key: dev/mdpus-client