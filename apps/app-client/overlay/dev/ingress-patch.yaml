apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mdpus-client-ingress
  annotations:
    cert-manager.io/cluster-issuer: acme-issuer
spec:
  ingressClassName: traefik-external
  rules:
    - host: mdpus-client-dev.poc.mdplabs.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mdpus-client
                port:
                  number: 3000
  tls:
    - hosts:
        - mdpus-client-dev.poc.mdplabs.dev
      secretName: mdpus-client-tls

