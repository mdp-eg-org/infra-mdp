apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sdk-backend-ingress
  annotations:
    cert-manager.io/cluster-issuer: acme-issuer
spec:
  ingressClassName: traefik-external
  rules:
    - host: placeholder-host
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sdk-backend
                port:
                  number: 4000
  tls:
    - hosts:
        - placeholder-host
      secretName: sdk-backend-tls