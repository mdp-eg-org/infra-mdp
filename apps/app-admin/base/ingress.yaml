apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-admin-ingress
  annotations:
    cert-manager.io/cluster-issuer: acme-issuer
spec:
  ingressClassName: traefik-external
  rules:
    - host: placeholder-host
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app-admin
                port:
                  number: 5000
  tls:
    - hosts:
        - placeholder-host
      secretName: app-admin-tls